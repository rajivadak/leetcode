
Coding problems:
	Arithemtic:
		Sum and Difference – Given two integers, find their sum and difference.
		Product and Quotient – Given two integers, compute their product and quotient (integer division).
		Modulo Operations – Compute the remainder when one number is divided by another.
		Power Calculation – Given two numbers, compute using pow() or a loop.
		LCM and GCD – Given two numbers, find their Least Common Multiple (LCM) and Greatest Common Divisor (GCD).
		Sum of Digits – Given an integer, find the sum of its digits.
		Reversing a Number – Reverse the digits of a given number without using strings.
		Factorial Calculation – Compute the factorial of a given number using recursion or loops.
		Check for Prime – Determine if a given number is prime or not.
		Average of Two Numbers – Find the average of two integers (handle floating-point precision).
		Nth Fibonacci Number – Compute the Nth Fibonacci number using recursion or an optimized approach.
		Number of Divisors – Count the number of divisors of a given integer.
		Sum of First N Natural Numbers – Compute the sum of the first N natural numbers using formulas.
		Perfect Number Check – Check if a number is a perfect number (sum of its divisors equals the number).
	15	Armstrong Number – Check if a number is an Armstrong number (sum of cubes of its digits equals the number).
	
	Conditional loops:
		Even or Odd – Given an integer, check if it is even or odd.
		Maximum of Three Numbers – Find the largest of three numbers using if-else.
		Grade Calculation – Given a student's marks, assign a grade (A/B/C/D/F).
		Leap Year Checker – Check if a given year is a leap year or not.
		Calculator Program – Implement a simple calculator using a switch-case statement.
		Sum of N Natural Numbers – Compute the sum of the first N natural numbers.
		Reverse a Number – Reverse the digits of a given number.
		Factorial Calculation – Compute the factorial of a number using a loop.
		Multiplication Table – Print the multiplication table of a given number.
		Number of Digits in an Integer – Count the number of digits in a given integer.
		Check for Prime Number – Determine if a number is prime or not.
		Armstrong Number – Check if a number is an Armstrong number (sum of cubes of digits = number).
		Fibonacci Series – Print the first N Fibonacci numbers using a loop.
		Sum of Digits – Compute the sum of all digits of a given number.
	30	Perfect Number Check – Check if a number is a perfect number (sum of divisors = number).
		
		
	Bitwise:
		Bitwise AND, OR, XOR – Given two integers, compute their AND, OR, and XOR values.
		Check if a Number is Even or Odd using Bitwise – Use the bitwise AND (&) operator to check if a number is even or odd.
		Swap Two Numbers Without Using a Third Variable – Swap two numbers using the XOR operator.
		Multiply a Number by 2 Without Using * Operator – Multiply a number by 2 using the left shift (<<) operator.
		Divide a Number by 2 Without Using / Operator – Divide a number by 2 using the right shift (>>) operator.
		Check if a Number is a Power of Two – Use bitwise operations to check if a number is a power of 2.
		Count the Number of Set Bits (Hamming Weight) – Count the number of 1s in a binary representation of a number.
		Find the Only Non-Repeating Element in an Array – Given an array where every number appears twice except one, find the unique element using XOR.
		Turn Off the Rightmost Set Bit – Given a number, turn off its rightmost 1-bit (change it to 0).
		Check if Two Numbers Differ by Only One Bit – Given two numbers, check if they differ by exactly one bit.
		Find XOR of All Numbers from 1 to N – Compute XOR of all numbers from 1 to N efficiently.
		Find the Position of the Rightmost Set Bit – Find the position (1-based) of the rightmost 1-bit in a number.
		Toggle a Specific Bit of a Number – Toggle the K-th bit of a number (flip from 0 to 1 or 1 to 0).
		Find Two Non-Repeating Elements in an Array – Given an array where every element appears twice except two, find those two unique numbers using XOR.
	45	Binary Representation of a Number – Print the binary representation of a given number without using built-in functions.
		
	Arrays:
		Find the Maximum and Minimum Element in an Array – Given an array, find the largest and smallest elements.
		Reverse an Array – Reverse the elements of an array without using extra space.
		Sum of All Elements in an Array – Compute the sum of all elements in an array.
		Find the Second Largest Element in an Array – Find the second-largest number in an array.
		Count Occurrences of an Element in an Array – Given an array and a number, count its occurrences.
		Check if an Array is Sorted – Determine whether an array is sorted in non-decreasing order.
		Left Rotate an Array by One Position – Shift all elements left by one position.
		Right Rotate an Array by K Positions – Rotate an array to the right by K positions.
		Move All Zeros to the End – Given an array, move all zeroes to the end while maintaining order.
		Find the Union and Intersection of Two Arrays – Compute the union and intersection of two arrays.
		Find the Missing Number in an Array – Given an array of size N-1 with numbers from 1 to N, find the missing number.
		Find the First Repeating Element – Find the first element that appears more than once.
		Find the Subarray with Given Sum – Given an array and a target sum, find a subarray with that sum.
		Kadane’s Algorithm (Maximum Subarray Sum) – Find the maximum sum of a contiguous subarray.
	60	Find the Leader Elements in an Array – An element is a leader if it is greater than all elements to its right.
		
	strings:
		Reverse a String – Reverse a given string without using built-in functions.
		Check if a String is Palindrome – Determine if a given string reads the same forward and backward.
		Convert a String to Uppercase and Lowercase – Implement functions to convert a string to uppercase and lowercase.
		Count Vowels and Consonants in a String – Given a string, count the number of vowels and consonants.
		Find the Length of a String Without Using strlen() – Implement a function to compute the length of a string.
		Check if Two Strings are Anagrams – Determine if two strings contain the same characters in a different order.
		Find the First Non-Repeating Character – Identify the first unique character in a string.
		Remove All Spaces from a String – Remove all whitespace characters from a given string.
		Count the Number of Words in a String – Given a sentence, count the total number of words.
		Check if a String Contains Only Digits – Verify if a given string consists of only numeric characters.
		Find the Longest Common Prefix Among Strings – Given an array of strings, find the longest common prefix.
		Find All Substrings of a String – Generate and print all possible substrings of a given string.
		Implement strstr() Function – Write a function to find the first occurrence of one string within another.
		Find the Most Frequent Character in a String – Determine the character that appears most frequently.
	75	Check if a String is a Rotation of Another – Determine if one string is a rotated version of another (e.g., "abcde" and "cdeab").

	vectors:
		Insert and Print Elements in a Vector – Read N integers and store them in a vector. Print all elements.
		Reverse a Vector – Given a vector, reverse its elements.
		Find Maximum and Minimum in a Vector – Find the largest and smallest elements in a vector.
		Sort a Vector – Sort the elements of a vector in ascending and descending order.
		Remove Duplicates from a Vector – Remove all duplicate elements and print the unique elements.
		Find the Frequency of Each Element in a Vector – Count occurrences of each unique element.
		Rotate a Vector to the Left by K Positions – Perform a left rotation on a vector by K positions.
		Find the Sum of All Elements in a Vector – Compute the sum of all elements.
		Find the Second Largest Element in a Vector – Find the second-largest number in a vector.
		Find the Intersection of Two Vectors – Given two vectors, find the common elements between them.
		Check if a Vector is Sorted – Determine whether a vector is sorted in non-decreasing order.
		Find the First Repeating Element in a Vector – Identify the first element that appears more than once.
		Implement Binary Search on a Sorted Vector – Search for an element in a sorted vector using binary search.
		Merge Two Sorted Vectors – Merge two sorted vectors into one sorted vector.
	90	Find the Longest Consecutive Sequence in a Vector – Given an unsorted vector, find the longest sequence of consecutive numbers.

	oops:
		Create a Class and Object – Define a Student class with attributes name, age, and marks, and create an object to display its values.
		Constructor and Destructor – Implement a Car class with a constructor to initialize brand and model and a destructor to print a message when the object is destroyed.
		Encapsulation – Implement a BankAccount class with private attributes for account balance and public methods for deposit and withdrawal.
		Getter and Setter Methods – Create a Rectangle class with private attributes length and width, and provide getter and setter methods.
		Static Members – Implement a Counter class where every object shares a static variable that keeps track of the number of objects created.
		Operator Overloading – Overload the + operator for a ComplexNumber class to add two complex numbers.
		Function Overloading – Implement function overloading in a MathOperations class to calculate the sum of two or three numbers.
		Inheritance (Single Inheritance) – Create a Person base class and a Student derived class with an extra attribute rollNumber.
		Multiple Inheritance – Implement a HybridCar class inheriting from ElectricCar and PetrolCar.
		Hierarchical Inheritance – Create a base class Animal and derive Dog and Cat classes from it.
		Abstract Class and Pure Virtual Function – Implement an abstract class Shape with a pure virtual function area(), then derive Circle and Rectangle classes.
		Function Overriding (Polymorphism) – Implement function overriding in a base class Employee and a derived class Manager to display different salary calculations.
		Friend Function – Use a friend function to allow access to private members of two different classes.
		File Handling in OOP – Create a FileHandler class with methods to write and read data from a file.
	105	Template Class – Implement a generic class Box<T> that can store any data type using templates.

	dynamic memory allocation:
		Allocate and Deallocate an Integer Dynamically – Use malloc/calloc/free in C and new/delete in C++ to create and delete an integer.
		Dynamic Array Allocation – Allocate memory for an array of N integers and take input dynamically.
		Resize an Array Using realloc() – Create a dynamic array and resize it using realloc().
		Create a 2D Array Dynamically – Allocate memory for a 2D array dynamically using pointers.
		Freeing Allocated Memory Properly – Implement a program that allocates memory and ensures it is properly freed.
		Reverse an Array Using Dynamic Memory – Store an array dynamically and reverse its elements.
		Concatenate Two Strings Using Dynamic Memory – Allocate memory for two strings and concatenate them dynamically.
		Find the Sum of N Numbers Using Dynamic Memory – Allocate memory for an array of N elements and compute the sum.
		Copy a String Using Dynamic Memory Allocation – Allocate memory and copy one string to another dynamically.
		Store and Process Student Records – Use structures and dynamic memory to store student data (name, age, marks).
		Implement a Stack Using Dynamic Memory – Create a stack using a dynamically allocated array.
		Implement a Linked List Dynamically – Create a singly linked list using dynamic memory allocation.
		Dynamic Memory Allocation for a Jagged Array – Allocate a jagged array dynamically where each row has a different number of columns.
		Create a Dynamic Matrix and Perform Addition – Allocate two matrices dynamically and compute their sum.
	120	Implement a Binary Tree Using Dynamic Memory – Create a binary tree using dynamically allocated nodes.
	
		recursion:
		Factorial of a Number – Compute the factorial of a given number n.
		Fibonacci Series – Print the first N Fibonacci numbers using recursion.
		Sum of First N Natural Numbers – Find the sum of numbers from 1 to N recursively.
		Reverse a String – Reverse a given string using recursion.
		Power Calculation – Compute using recursion (exponential function)
		Check if a String is Palindrome – Determine if a given string is a palindrome using recursion.
		Greatest Common Divisor (GCD) – Find the GCD of two numbers using Euclidean recursion.
		Count the Number of Digits in an Integer – Count the total digits in a number using recursion.
		Print an Array in Reverse Order – Print all elements of an array in reverse order using recursion.
		Find the Maximum Element in an Array – Find the maximum element in an array recursively.
		Tower of Hanoi – Solve the Tower of Hanoi problem for N disks.
		Binary Search Using Recursion – Implement recursive binary search in a sorted array.
		Generate All Subsets of a String (Power Set) – Generate all subsets (power set) of a given string.
		Josephus Problem – Solve the Josephus problem using recursion.
	135	Sudoku Solver – Implement a recursive backtracking approach to solve a Sudoku puzzle.
	
Done: 1 70 38 52 108 28 14 106 76 29 39 19 4 126 9

Diff: 133 71 118 90 117
	
                 20 
22 3 132 134 31 5 25 59 69 55 119 65 8 46 13 43 84 94 127 12 107 
131 26 97 101 35 47 11 72 68 103 98 81 10 87 93 73 66 54  49 44 
110 51 23 83 130 114 77 111 15 42 80 24 91 82 135 86 16 120 92 
75 41 45 113 27 17 99 89 58 74 129 6 95 123 100 104 109 67 102 
32 112 85 36 62 48 37 122 33 64 2 34 50 125 79 57 105 121 63 40 
53 56 78 128 124 61 18 96 88 115 7 21 60 116 30 

=====================================================================================================================================================
=====================================================================================================================================================
=====================================================================================================================================================

Blockchain programming plan:


### **Step 1: Master Linked Lists and Pointers in C/C++**
Since blockchain systems require efficient memory handling and low-level manipulation, **C and C++** are ideal for learning these concepts.  

#### **Topics to Cover**
1. **Pointers**
   - Basics: Pointer declaration, dereferencing, pointer arithmetic  
   - Dynamic memory allocation (`malloc`, `calloc`, `free`, `new`, `delete`)  
   - Pointer to pointer (`**ptr`) and pointer to structures  

2. **Linked Lists**
   - Singly Linked List (Insertion, Deletion, Traversal)  
   - Doubly Linked List (Forward and Backward Traversal)  
   - Circular Linked List  
   - Hash-linked structures (used in blockchain)  

---

### **Step 2: Implement a Basic Blockchain using Linked Lists**
Once you're comfortable with pointers and linked lists, build a **simple blockchain prototype** where each block is a node in a linked list.

#### **Basic Blockchain Node Structure in C**
```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct Block {
    int index;
    char data[256];   // Transaction data
    struct Block* prev;  // Pointer to the previous block
} Block;

// Function to create a new block
Block* createBlock(int index, const char* data, Block* prevBlock) {
    Block* newBlock = (Block*)malloc(sizeof(Block));
    newBlock->index = index;
    strcpy(newBlock->data, data);
    newBlock->prev = prevBlock;  // Linking to previous block
    return newBlock;
}

// Function to print the blockchain
void printBlockchain(Block* block) {
    while (block) {
        printf("Block %d: %s\n", block->index, block->data);
        block = block->prev; // Traversing the blockchain
    }
}

int main() {
    Block* genesis = createBlock(0, "Genesis Block", NULL);
    Block* block1 = createBlock(1, "Transaction 1", genesis);
    Block* block2 = createBlock(2, "Transaction 2", block1);

    printBlockchain(block2);

    // Free allocated memory
    free(block2);
    free(block1);
    free(genesis);
    return 0;
}
```
✅ **Key Takeaways**:
- `prev` pointer links the current block to the previous one (like a linked list).
- Memory is allocated dynamically (`malloc` and `free`).

---

### **Step 3: Move to a Blockchain-Specific Language**
Once you're comfortable with C/C++, you can explore **Go, Rust, and Solidity**, which are widely used in blockchain.

#### **Languages to Learn for Blockchain Development**
| **Language**  | **Use Case** |
|--------------|-------------|
| **C++**      | Used in **Bitcoin Core**, fundamental for blockchain logic |
| **Go**       | Used in **Hyperledger Fabric**, easy concurrency & networking |
| **Rust**     | Used in **Solana, Polkadot**, secure memory management |
| **Solidity** | Used in **Ethereum Smart Contracts**, works with EVM |
| **Python**   | For rapid prototyping and blockchain scripting (e.g., Web3.py) |

🔹 **Recommended Choice**:  
- Start with **C++ (for low-level blockchain concepts)**  
- Learn **Go (for modern blockchain systems)**  
- **Rust (for security-focused chains like Solana)**  
- Move to **Solidity** (for smart contract development)  

---

### **Step 4: Follow Structured Learning Resources**
📚 **Books & Online Courses**  
1. **Pointers & Linked Lists**
   - "The C Programming Language" – Brian Kernighan & Dennis Ritchie  
   - "Data Structures and Algorithm Analysis in C" – Mark Allen Weiss  
   - "Understanding Pointers in C" – Yashavant Kanetkar  

2. **Blockchain Basics**
   - [Mastering Bitcoin by Andreas Antonopoulos](https://github.com/bitcoinbook/bitcoinbook)  
   - [Blockchain Basics (Coursera)](https://www.coursera.org/learn/blockchain-basics)  
   - [Ethereum & Solidity Course (Udemy)](https://www.udemy.com/course/ethereum-and-solidity-the-complete-developers-guide/)  

3. **Hands-on Blockchain Development**
   - **Bitcoin:** Read Bitcoin Core source code (written in C++) [GitHub](https://github.com/bitcoin/bitcoin)  
   - **Ethereum:** Learn Solidity & Ethereum Virtual Machine (EVM)  
   - **Hyperledger Fabric (Go-based blockchain)** [Hyperledger Fabric Docs](https://hyperledger-fabric.readthedocs.io/en/latest/)  

---

### **Step 5: Practice with Blockchain Projects**
✅ **Mini Projects to Try**
1. **Basic Blockchain in C++** (With SHA-256 hashing)  
2. **Build a Linked List-based Blockchain in Go**  
3. **Write a Smart Contract in Solidity**  
4. **Implement a Proof-of-Work System in Rust**  

Would you like a **Go-based blockchain implementation** next? 🚀

=====================================================================================================================================================
=====================================================================================================================================================
=====================================================================================================================================================

ethical_hacking_notes

Starting plan:
	1. Understand the Basics of Networking and Security

	Networking Concepts: Learn about TCP/IP, subnets, firewalls, and protocols (HTTP, FTP, DNS, etc.).
	Security Fundamentals: Familiarize yourself with concepts such as encryption, authentication, and common vulnerabilities (like OWASP Top Ten).
	
	2. Learn About Operating Systems
	
	Linux: Get comfortable with Linux, as many hacking tools and servers run on it. Start with a distribution like Ubuntu or Kali Linux.
	Windows: Understand Windows internals, as many targets are Windows-based.
	
	3. Programming and Scripting Skills
	
	Python: It's widely used in hacking for scripting and automation. Learn how to write scripts to automate tasks.
	JavaScript: Useful for web application hacking and understanding client-side vulnerabilities.
	
	4. Familiarize Yourself with Hacking Tools
	
	Kali Linux: A distribution specifically designed for penetration testing with pre-installed tools (like Metasploit, Nmap, Burp Suite).
	Wireshark: For network analysis and packet sniffing.
	Burp Suite: For web application security testing.
	
	5. Hands-On Practice
	
	Capture The Flag (CTF) Competitions: Join platforms like Hack The Box, TryHackMe, or OverTheWire to practice your skills in a controlled environment.
	Vulnerable Applications: Use intentionally vulnerable applications like DVWA (Damn Vulnerable Web Application) or OWASP Juice Shop to practice exploiting vulnerabilities.
	
	6. Learn Ethical Hacking
	
	Courses: Consider taking online courses or certifications like Certified Ethical Hacker (CEH) or Offensive Security Certified Professional (OSCP).
	Books: Read books like "Hacking: The Art of Exploitation" by Jon Erickson or "The Web Application Hacker's Handbook" by Dafydd Stuttard and Marcus Pinto.
	
	7. Stay Updated and Join Communities
	
	Forums and Communities: Engage with forums like Reddit’s r/hacking, Stack Overflow, or specialized Discord servers.
	Follow Blogs and Podcasts: Stay current by following cybersecurity blogs and podcasts.
	
	8. Ethical Considerations
	
	Always practice hacking ethically. Ensure you have permission before testing systems and adhere to the laws and regulations in your area.
	Conclusion
	
	Learning hacking is a continuous journey that combines theory with practical skills. With your knowledge of C and C++, you already have a strong foundation for understanding how software works, which is a significant advantage in your hacking education. Good luck!

Next action plan:
  - continue the course of c++ ethical hacking, study linux book on work-laptop in office, and practice project on mac.
  
=====================================================================================================================================================
=====================================================================================================================================================
=====================================================================================================================================================

Cpp in blockchain development

Next action:

- decide a small and simple project for initializing of blockchain development
  project 1: Basic Blockchain implementation
  Create a simple blockchain that stores data in blocks. Each block should contain: index,timestamps,data,previous block's hash,current block's hash
  outcome: understand how blocks are linked together using hashes
  tools: use c++ libraries like <openssl/sha.h> for hashing    

=====================================================================================================================================================
=====================================================================================================================================================
=====================================================================================================================================================

C++ in game development 

Books: SFML Game development by example,
Beginning C++ through game programming


SFML - simple and fast multimedia language

game designing is different from game programming - search on that


Game engines - learning to make one from scratch in this course
	
ECS Game programming - Entities, Components, Systems.
	Is a software design paradigm, and can be used for all software, not limited to gaming only

Object Oriented Programming - 
	Object Oriented programming used the concept of Objects and Inheritance to implement specific functionality
	Object Oriented design (in games) is the process of strictly using OOP in order to design the architecture of the game engine and the entities within it.
	
	Entity - Anything object in the game: player, platform, tile, design
	Component - properties attached to entities
		Components are pure data
	Systems - Code/logic that drives the behaviour in the game
		movement, rendering, sound, physics 
		
Engine Architecture:

	Game Engine
		> Scene
			> Systems
			> EntityManager [Factory data manager] ***important***
				> Entity
					> Component
					
=====================================================================================================================================================
=====================================================================================================================================================
=====================================================================================================================================================

5/2/25 Wednesday

C programming

Bitwise operators

	- bitwise operator and shift operators
	- operates in bits in integer values

Cast operator: the type cast operator has a higher precedence than all the arithmetic operators except the unary minus and unary plus operator
	(int)21.51+(int)26.99 -> 21+26

sizeof operator:
	evaluated at compile time and not runtime, unless a variable-length array is used in its argument

Operator precedence:

highest to lowest as we go to the bottom-

Postfix: () [] -> . ++ -- : Left to right
Unary: + - ! ~ ++ -- (type) * & sizeof : Right to Left 
Multiplicative: * / % : Left to right
Additive: + - : Left to right
Shift: << >> : Left to right
Relational: < <= > >= :Left to right
Equality: == != : Left to right
Bitwise AND: & : Left to right
Bitwise XOR: ^ : Left to right
Bitwise OR: | : Left to right
Logical AND: && : Left to right
Logical OR: || : Left to right
Conditional: ?: : Right to Left
Assignment: = += -= *= /= %= >>= <<= &= ^= |= : Right to Left
Comma: , : Left to right

6/2/25 Thursday

C++ language:
	
	C++ Classes:
		
		class Point
		{
			int m_x = 0;	//private[hungarian notation]
			int m_y = 0;	//private[hungarian notation]
		public:
			Point(int x, int y);
			int getX();
			int getY();
		};

DSA programming

	Pointers:
		- the program can directly access the code area and the stack memory
		- program cannot acces the heap memory
		- Heap memory is external to the program
		- So we use pointer to access the Heap memory
		- Major advantages of the pointers is accessing the resources which are outside of the program
		- also pointers are used for passing parameters
		- whenever we declare a variable, it will be inside stack
		- we use malloc to allocate/access memory in heap
		- to use malloc, we need to use #include <stdlib.h>
		- syntax of malloc
			int *p;
			//p = (int*)malloc(<size of array>*sizeof(<datatype>));
			p = (int*)malloc(5*sizeof(int)); //syntax in c language
			p = new int[5]; //syntaxin c++
		- as malloc returns void, so we are type casting malloc with (int*)
		- (int*) as in for integer pointer
		- We use the * in pointer only two times, once during declaration and once during dereferencing the pointer
		- We don't use "&" when we are giving any array name to a pointer, so we can declare an array with pointer as given below:
			int *p;
			int A[5]={........};
			p=A; //correct syntax
		- when we dynamically creating/allocating any memory in heap, we must release the memory using the keyword 
			delete [ ]p; // in c++
			free(p);     // in c
		- we will be using this delete/free feature when the programs are too long and extensive
		- Every pointer takes same amount of memory
		- Size of pointer is independent of the datatype
		
	Reference
		-only available in c++, not available in any other language
		-a nickname given to a variable
		- suppose the below code:
			int a=10;
			int &r=a; //here r is referenced to a, i.e r is the nickname for a
		-having reference in c is useful for passing parameters
		-instead of pointers, we can use references in small programs
		- references must be initialized with the value at the same time when it is declared
		- reference is not the same as a pointer
	
C++ in game_dev comp4300

	Books: SFML Game development by example,
	Beginning C++ through game programming
	
	SFML - simple and fast multimedia language
	
	game designing is different from game programming - search on that
	
	
	Game engines - learning to make one from scratch in this course
		
	ECS Game programming - Entities, Components, Systems.
		Is a software design paradigm, and can be used for all software, not limited to gaming only
	
	Object Oriented Programming - 
		Object Oriented programming used the concept of Objects and Inheritance to implement specific functionality
		Object Oriented design (in games) is the process of strictly using OOP in order to design the architecture of the game engine and the entities within it.
		
		Entity - Anything object in the game: player, platform, tile, design
		Component - properties attached to entities
			Components are pure data
		Systems - Code/logic that drives the behaviour in the game
			movement, rendering, sound, physics 
			
	Engine Architecture:
	
		Game Engine
			> Scene
				> Systems
				> EntityManager [Factory data manager] ***important***
					> Entity
						> Component
	
10/2/25 Monday

Pointers

Smart Pointers:
	- they manage the memory on their won and don't require the memory to be freed manually
	- the smart pointers in c++ are:
		1. std::unique_ptr:
		2. std::shared_ptr:
		3. std::weak_ptr:
		4. std::auto_ptr: deprecated in c++11 and removed in c++17
		
11/2/25 Tuesday

Notes/Cpp_dev_requirements

	Programming Requirements
	
	1. CPP - Master C++11/14/17 features
	2. DSA
	3. OOPS
	4. Multithreading and Concurrency
	5. Template Metaprogramming
	6. Memory management - pointers, dynamic memory allocation, smart pointers
	7. STL, vectors, maps, sets, iterators and standard libraries
	8. Lambda expressions and funtional programming
	9. Low latency and high performance programming
	10. Mutex and semaphores
	
	Low latency and high performance programming
	
	1. Cache Optimization
	2. SIMD Instructions
	3. Lock-free programming
	4. Paralled Programming

Blockchain development roadmap: copy to the roadmap/plan file

	Types of blockchain development:
		1. Application devs - solidity, javascript, python
		2. Core devs - golang, c++, rust
	What will you do?
		- smart contracts on ethereum BC using solidity
		- understanding c++ through blockchain development
	Tools & tech:
		1. javascript - react, node, express, web3
		2. Truffle[framework]
		3. Ganache - for hosting and deploying applications [test transactions] 
		4. Metamask
	What more is required? [Tutorials -> projects -> work]
		- Web development
		- Crypto graphy
		
14/02/25 - Friday

Linux - [from book]
	The kernel also maintains data structures that map the virtual memory of each process into the physical memory of the computer and the swap area(s) on disk. All communication between processes is done via mechanisms provided by
	the kernel. In response to requests from processes, the kernel creates new pro-
	cesses and terminates existing processes. Lastly, the kernel (in particular, device
	drivers) performs all direct communication with input and output devices, transfer-
	ring information to and from user processes as required.
	
	
	Difference between normal link and symbolic link in linux
	
	The stdio library
	To perform file I/O, C programs typically employ I/O functions contained in the
	standard C library. This set of functions, referred to as the stdio library, includes
	fopen(), fclose(), scanf(), printf(), fgets(), fputs(), and so on. The stdio functions are
	layered on top of the I/O system calls (open(), close(), read(), write(), and so on).
	
	
	Process memory layout
	A process is logically divided into the following parts, known as segments:
	z Text: the instructions of the program.
	z Data: the static variables used by the program.
	z Heap: an area from which programs can dynamically allocate extra memory.
	z Stack: a piece of memory that grows and shrinks as functions are called and
	return and that is used to allocate storage for local variables and function call
	linkage information.
	
	An execve() call destroys the existing text, data,
	stack, and heap segments, replacing them with new segments based on the code of
	the new program
	
	Process termination and termination status
	A process can terminate in one of two ways: by requesting its own termination
	using the _exit() system call (or the related exit() library function), or by being killed
	by the delivery of a signal. 
	 
	
Linux - [how to study from youtube]
	- man pages
	- tldr pages
	- arch wiki
	- gentoo linux
	- check starred repos[linux_dev]

Quant - resources
	- Trading systems developers guide - jeff vogels
	
	
=====================================================

17/02/25 - Monday	

Books for Cpp:
	Daily c++ interview Sandor dargo
	C++ software design Klaus lglberger
	C++ concurrency in action Anthony Williams
	TCP/IP illustrated volume 1 Kevin R fall
	Inside the machine Jon Stokes
	Operating systems 3 easy pieces remzi & andrea
	Beautiful c++ guy davidson
	Design patterns erich gamma, richard helm
	C++ 17 in detail Bfil
	
C projects:
	recreating printf function
	creating a simple shell
	building a monty byte code interpreter
	building simple http web server
	
CPP concurrency:
	
	Creating a thread:
		
	[c++ 11/14/17]:
		#include <thread>
		void work(){
			//do some work
		}
		int main(){
			std::thread t(work); //thread starts executing the process at once
			t.join(); //detach
		}
		
	[c++ 20]:
		#include <thread>
		void work(){
			//do some work
		}
		int main(){
			std::jthread t(work); //thread starts executing the process at once
		}
		
	Note: difference between thread and jthread is the jthread object will join in its destructor. We don't need to explicitly use join.
		
	There are 2 ways of managing a thread in cpp, one way is to use the keyword "join", second way to use "detach"
	If we want a not blocking behaviour, we need to detach the thread
	
	Creating a thread and passing arguments:
	
		c++11/14/17:
			std::thread t(work, arg1, arg2);
		
		c++20:
			std::jthread jt(work, arg1, arg2);
			
18/02/25 Tuesday

C++ programming:

	Global variable: 
		It can be accessed any where in the code. However, a local variable has a higher precedence than a global variable.
	
	Primitive Data types:
		Using listed initialization is better than normal initialization, as during compile time itself the error will be shown incase of an overflow incident.
		
		long people_on_earth {7'600'000'000}  //this will give compile error of overflow
		long people_on_earth = 7'600'000'000  //this will not show error during compile, however, this will give wrong output due to overflow
		
DSA:
	
	Pointer to a structure:
		- This is a static object
		- Code:
			struct Rectangle
			{
				int length;
				int breadth;
			};
			int main()
			{
				struct Rectangle r={10,5};  // C language code, where the keyword struct is mandatory
				Rectangle r={10,5};			// CPP language
				struct Rectangle *p = &r; //pointer to a structure
				r.length = 15;  //modifying parameter
				(*p).length=20  //Method 1
								//This is modifying the parameter using a pointer
								//we put (*p) as the "." operator has a higher precedence over "*" operator, and without the "()", the expression will evaluate to =>*(p.length), which is wrong.
				p->length=20    //Method 2
								//we use the -> operator to point the pointer to the parameter p
				cout<<p->length<<endl;
				cout<<p->breadth<<endl;
				
				return 0;
			}
		
		Creating object dynamically in the heap using pointer:
		- This is a dynamic object
		- Code:
			struct Rectangle
			{
				int length;
				int breadth;
			};
			int main()
			{
				struct Rectangle *p;
				Rectangle *p;		//in CPP, it is valid to remove the keyword struct when declaring a struct in int main(){}
									// However, in C language, the struct keyword is mandatory
				p=(struct Rectangle *)malloc(sizeof(struct Rectangle)); //in C language
				p=new Rectangle;    //CPP language
				
				/*
				when in C, we need to typecast malloc to 
				the structure Rectangle pointer type, 
				as malloc returns a void pointer
				*/
				
				p->length=15;
				p->breadth=7;
				cout<<p->length<<endl;
				cout<<p->breadth<<endl;
				
				return 0;
			}
			
	Functions:
		- In C language, only "pass by value" and "pass by address" is present
		- However, in CPP, along with the above 2, there is "pass by Reference" also, so total 3
		
	Parameter passing methods:
		Call by value:
			When you don't have to modify actual parameters, we use pass by value
			We use this when we want a function to process a value and return the result, without actually modifying the actual parameters.
		Call by address:
			In pass by address, the formal parameter should be of the type pointers.
			And the parameters passed to the function from main fucntion should of the type address.
			It uses pointers
			One function cannot access the variables of other functions, but it can do so using pointers
		Call by reference:
			Need to pass references of actual parameters to the external function. eg[a->&x,b->&y]
			When reference parameters are manipulated, the actual parameters are also modified
			reference is nothing but a nickname for the variable it is referencing
			
	Array as parameter:
		Array is passed as an address, they cannot be passed by value.
		The other parameter passed to the function represents the size of the array, which is passed by value.
		In this case, modifying the values in call function will modify the values in main function parameter, as it is called by address.
		Code:
			// void fun(int B[], int n) //method 1
			void fun(int *B, int n)     //method 2, 
			/*
			instead of using array like B[], 
			we can use *B. As this *B can point 
			to an integer as well as to an array.
			So using B[] also means a pointer to 
			an array
			*/
			{
				int i;
				for(int i=n-1; i>=0; i--)
				{
					printf("%d ", B[i]);
				}
			}
			
			int main()
			{
				int A[] = {2,4,6,8,10};
				fun(A,5); // here address of A is passed to the above function fun
				
				return 0;
			}
			
	Returning Array as a parameter:
		code:
			#include <iostream>

			int * fun(int n) 	//this is passing by value, the size of array
								//[] represents returning an array
								//You can use * instead of []
			{
				int *p;
				p = (int*)malloc(n*sizeof(int));
				return p;		//returning pointer to the newly created array in the heap
			}
			
			int main()
			{
				int *A;
				A = fun(5);		//passing by value
								//the size of the array required
				return 0;
			}
			
	Structure as a parameter:
		Passing struct as a call by value:
			When we pass struct object as a parameter to some other function, a separate object is created by that function. It does not use the pre-existing object from main function. So only the formal parameters change, not the actual parameters.
		Passing struct as a call by reference:
			Compared to the above explaiantion, in this case separate object is not created, and the actual parameters are modified.
		Passing struct as a call by address:
			when we want a function to change the actual parameters, we use call by address or call by reference.
		Code:
			struct Rectangle
			{
				int length;
				int breadth;
			};
			
			int area_value(struct Rectangle r1)   //call by value, does not change the actual parameters
			{
				return r1.length*r1.breadth;
			}
			
			int area_reference(struct Rectangle &r1)  //call by address, changes the actual parameters
			{
				return r1.length*r1.breadth;
			}
			
			void change_length(struct Rectangle *r1, int l)  //call by address, changes the actual parameters
			{
				r1->length=l;                   //We use pointer to a structure, as change_length has *r1 passed from address in main function
				
			}
			
			int main()
			{
				struct Rectangle r = {10,5};
				
				change_length(&r, 20);    //passing struct as an address
				
				printf("area_value is %d \n", area_value(r));
				printf("area_reference is %d \n", area_reference(r));
				//printf("area_address is %d \n", area_address(r));
			
				return 0;
			}
		
		[imp] We know that we cannot pass an array to function as value, we can only pass an array as n address. 
		However, suppose, if a structure has an array in it, we can pass that struct to a function as a value. 
		So, individually an array can't be passed by value, but a struct containing an array can be passed by value.
		
		Code 2:
			struct Rectangle
			{
				int length;
				int breadth;
			};
			
			//call by value
			void fun(struct Rectangle r1)
			{
				r1.length=20;
				r1.breadth=10;
				cout<<"value_length "<<r1.length<<endl<<"value_breadth "<<r1.breadth<<endl;
			}
			
			//call by address
			void fun_address(struct Rectangle *r2)
			{
				r2->length=30;
				r2->breadth=40;
				cout<<"add_length "<<r2->length<<endl<<"add_breadth "<<r2->breadth<<endl;
			}
			
			
			int main()
			{
				struct Rectangle r = {10,5};
				printf("p_length %d\np_breadth %d\n", r.length, r.breadth);
				fun_address(&r);
				fun(r);
				return 0;
			}
			// Output:
			// p_length 10
			// p_breadth 5
			// add_length 30
			// add_breadth 40
			// value_length 20
			// value_breadth 10
		
		Returning address of a structure:
			code 3:
			struct Rectangle
			{
				int length;
				int breadth;
			};
			
			struct Rectangle *fun()  //this struct pointer will be created in heap
			{
				struct Rectangle *p;
				p=new Rectangle;
				//p=(struct Rectangle *)malloc(sizeof(struct Rectangle)); //c-lang
				p->length=15;
				p->breadth=7;
				return p;
			};
			
			int main()
			{
				struct Rectangle *ptr=fun();;
				cout<<"lenght "<<ptr->length<<endl<<"breadth "<<ptr->breadth<<endl;
				return 0;
			}
	
	Class and constructor:
		Every cpp class should have a constructor.
		for initial times, there are two constructors, default and parameterized constructor. 
		In parameterized constructor we initialize the paramters which we will be passing.
		Converting c to c++ code:
			Like how we create ine structure with data-members in C, and make different functions and initializations around it, in c++ we create classes with these data-members, functions & initializations.
			When we want to initialize the private data-members in the class, we can't do that by using any other function-member in that class.
			So we use a constructor, whoes name is same as that of class name, and which initializes the private-data-members. 
			So this also creates the object of that class.
			
	Template Classes:
		code:
			
			template<class T>		//template starts here and ends at the next curly bracket
			class Arithametic
			{
				private:
					int a;
					int b;
				public:
					Arithametic(int a, int b);
					int add();
					int sub();
			}
			
			template<class T>  //as we are using class name as a scope resolution and passing it to template, we need to pass the template name through the class name as a part of scope resolution
			Arithametic<T>::Arithametic(T a,T b)
			{
				this->a=a;	//this is a pointer to the current object
				this->b=b;
			}
			
			template<class T>
			int Arithametic<T>::add()
			{
				T c;
				c = a+b;
				return a+b;
			}
			
			template<class T>
			int Arithametic<T>::sub()
			{
				T c;
				c=a-b;
				return a-b;
			}
			
			int main()
			{
				Arithametic<int> ar(10,5);  //here once we have declared the Arithametic type as int, it replaces all the places where we have typed "T"
				cout<<ar.add();
				
				Arithametic<float> ar1(1.5,1.2);
				cout<<ar1.add(); 
			}
			
			
			
	
			
	Linked_lists:
		Arrays have difficulty in updating, adding or deleting items in the list, the use of linked lists comes in place.
		Linked lists are inferior to array, as they are bad at searching, while in array we just need to find the address of the starting element.
		
		Singly linked lists:
			Let's consider each memory space where data is stored as a "NODE".
			So in arrays, each NODE has two components, one is the node data and 2nd is the node index. In arrays, all the nodes are in a contiguos manner.
			However, in linked lists, each node has a data and address of the next node. In this way, the nodes in a linked lists need not be in a contiguous manner.
			So whenever we need to update, add, delete any data in a linked list, we need not re-arrange all the data, we just need to update the address stored in the subsequent nodes.
			While in array, we need to move all the subsequent nodes while doing the same.
		
		Continue the video:https://www.youtube.com/watch?v=N6dOwBde7-M
		from 4:10
		
C++ in game_dev comp4300:
	
	C++ classes:
		- default access specifier in a class is private
		- code:
			
			class Point
			{
				int m_x = 0;    //private
				int m_y = 0;
			public:
				Point(int x, int y);
				int getX();
				int getY();
			};
			
	C++ class constructor:
		- code:
			Point::Point( int x, int y)
			{
				m_x = x;
				m_y = y;
			}
			
		- member initializer lists:
			//constructor
			Point::Point(int x, int y)
				: m_x(x)
				, m_y(y)
			/*
			Even before we get to the below {} code, we set up the private members m_x & m_y with their respective initializations.
			So if we want to set the const variable list with some other vlaue initialization, we need to use the member initializer lists
			*/
			{
				//rest of the code comes here
			}
			//destructor
			Point::~Point()
			{
				//clean-up here
			}
			
	STL Containers:
		Most commonly used:
			std::vector<T>  //vector of type T
							//T must have a default constructor
			std::set<T>     //ordered set
							//T must have < operator defined
			std::map<T1,T2> //map from T1 to T2
			
	C++ vector:
		Code 1:
			#include <iostream>
			#include <vector>
			
			int main(int argc, char * argv[])
			{
				std::vector<int> vec;
				vec.push_back(42); //vec is stack data-structure so we are using  push_back
				vec.push_back(10);
				
				std::cout<<vec[0]<<"\n";
				std::cout<<vec[1]<<"\n";
				std::cout<<vec[2]<<"\n";
				/*
				Now what happens in the above cout, as vec[2] is not defined, 
				so the cout of vec[2],  goes out of bound, so in this case the 
				compiler takes what ever value is presnt at the undefined vector 
				index at the time of compiling.
				*/
				
				return 0;
			}
			
		Code 2:
			#include <iostream>
			#include <vector>
			
			int main(int argc, char * argv[])
			{
				std::vector<int> vec;
				vec.push_back(42); 
				vec.push_back(10);
				
				for(int i=0; i<vec.size(); i++){
				std::cout<<vec[i]<<"\n";
				}
				
				/*
				The above for loop will give a warning, as we are comparing
				int i [signed int type] with vec.size() [unsigned int type].
				So iterating through a type vector, the vector size will be 
				of the type unsigned, so it is better to use size_t for any
				other int, to ensure that it is also of the type unsigned int.
				The correct syntax is given below:
				*/
				
				for(size_t i=0; i<vec.size(); i++){
				std::cout<<vec[i]<<"\n";
				}
				
				return 0;
			}
			
		Code 3:
			#include <iostream>
			#include <vector>
			
			int main(int argc, char * argv[])
			{
				std::vector<float> vec;
				vec.push_back(42.2f); 
				vec.push_back(10.1f);
				vec.push_back(11.3f);
				
				for(auto a : vec){
					std::cout<<a<<"\n";
				}
				/* Basically, using the auto keyword in the above for loop
				make sure the "a" in for loop is returned as float type.
				
				Need to take care as the data-type which is matched for "a" above
				is copied to the vector "vec" as well. This can be particularly
				problematic as it can change the data-type of vector for 
				further functions also.
				*/
				return 0;
			}
		code 4:
			
			class Student
			{
				std::string m_first = "first";
				std::String m_last = "last";
				int m_id = 0;
				float m_avg = 0;
				
			public:
				
				Student(){}  //default constructor
				
				Student(std::string first, std::string last, int id, float avg)
					: m_first(first)
					,m_last(last)
					,m_id(id)
					,m_avg(avg)
				{
				}     //parameterized constructor
				int getAvg()
				{
					return m_avg;
				}
				int getID()
				{
					return m_id;
				}
				str::string  getFirst()
				{
					return m_first;
				}
				str::string getLast() const 
				{
					return m_last;
				}
				
				void print(){
					std::cout << m_first <<" ">m_last<<>
				}
			}
			
			int main(){
				Student s1;
				Student s2("dave","churchill", 1,3.14);
				Student s3("jane", "doe", 202200001, 99.9);
				
				return 0;
			}
		
		Don't try and change an object within a const function. Also, we cannot pass a constant argument in a function which is func that is not a const.
		So, if you have any functions within any class which is not changing the internals of the object, it's a good practice to make it const.
			
	
			
Quant_dev:
	
	Options, Futures and other derivative - John C Hull
	
	Chapter 1: Introduction
		Types of markets:
			1. Exchange traded markets
			2. Over-the-counter markets
			3. Forward contracts
			4. Future contracts
			5. Options
			6. Types of traders
			7. Hedgers 
			8. Speculators
			9. Arbitrageurs
			10. Dangers
			
		1.5 Options:
			A call option gives the holder the right to buy the underlying asset by a certain date for a certain price.
			A put option gives the holder the right to sell the underlying asset by a certain date for a certain price.
			The price of a call option decreases as the strike price increases
			The price of a put option increases as the strike price increases. 
			Both types of option tend to become more valuable as their time to maturity increases
			Types of participants in options market:
				1. Buyers of calls
				2. Sellers of calls
				3. Buyers of puts
				4. Sellers of puts.
		
		Next action:
			-continue with the jon hull book
			-start reading quantlib docs
			
=====================================================
19/02/25 Wednesday

C programming:
	
	2D arrays:
		int numbers[3][4] = 
			{
				{..........}, //first row
				{..........}, //second row
				{..........}  //third row
			};
		
		It is not required to initialize all elements in a 2D array
		Rest of the elements are initialized to 0.
		
		Designated initializers:
			int numbers[3][4] ={[0][0]=1, [1][1]=5, [2][2]=9}; //rest elements are initialized to 0
	
	Variable length arrays:
		The term variable length array does not mean that you can modify the length of the array after you create it
		A VLA keeps same size after creation
		A VLA allows you to specify the size of an array with a variable when creating an array.
		You cannot initialize a VLA in its declaration.
			float a8[n];
			float a9[m];
	
	Functions:
		A function is a self-contained unit of program code designed to accomplish a particular task.
		[imp] When passing an array as an argument to a function, you must also pass an additional argument specifying the size of the array. 
		The function has no means of knowng how many elements are there in the array.
		When invokinga function, the arguments you specify when you call a function should agree in type, number, and sequence with which the paramters are passed in the function header.		
		
	Strings:
		Single char is assigned between '<>'
		String constant or string literal is a sequence of characters or symbols b/w pair of double-quote characters "<>"
		A string is always terminated by a null char, so the length of string is always one greater than the number of charachters in the string.
		We cannot assign strings directly to charachter arrays in c-lang, however, we can declare individual chars in the declared charachterarray with single char literals.
			char s[100];
			s="hello"; //doesnt work
			
			s[0]='h'; //this works

	Character String:
		char myString[20];  //in this we can store a string of only 19 length, last one is for the null char "\0"
		char word[] = {'H','E','L','L','O','!'};
		
	giving string as input:
		char input[10];
		scanf("%s",input);  //we dont need the & operator to use on string while using scanf
		
	Constant strings:
		#define BEEP '\a'
		#define TEE 'T'
		#define ESC '\033'
		
		C has a way yo create symbolic constants using 'enums'
		
		const char message[]="<string>";
	
	Common string functions:
	Need to used <string.h> library
		strlen - length of a string
			returned as size_t
		strcpy() & strncpy()- copying one char string to another
			strcpy does not check whether the source string fits in the target string or not, so strncpy is the safer choice which checks
		strcat() & strncat()- concatenation of 2 strings
		strcmp() & strncmp()- comparing two strings
		

	
C++ programming:

	Threads:
		Code 1:
			#include <iostream>
			#include <thread>
			
			static bool s_finished = false;
			
			void DoWork()
			{
				while(!s_finished)
				{
					std::cout << "working..... \n";
				}
			} 
			
			int main()
			{
				
				std::thread worker(DoWork);     //1. Thread is opened, and work starts executing in the DoWork function
				
				std::cin.get();					//2. While the above thread continuosly prints, this other thread wait for an input "enter" from the user
				s_finished = true;				//3. Once the user presses enter, the falg becomes true, and this make the above print statement stop
				
				worker.join();					//4. The above execution wait until point 1, until the user first presses enter,
												// after that it executes point 2 & 3, and then after that it come to point 4, and not the thread stops.
				
				std::cin.get();					//5. Once the user presses enter again, the program terminates
			}
			
	Arrays and vectors:
		initialization of an array:
			data_type array_name [number of elements] {initialization list};
			eg:
			int test_scores [5]{100,99,98,97,95};
			
	Vectors:
		Arrays never  do bound checking.
		Many vector methods provide bound checkings
	
		Declaring a vector:
			#include <vector>
			using namespace std;
			
			vector <char> vowels {'a','e','i','o','u'};
			vector <int> test_scores{100,98,99,97,95};
			vector <double> hi_temp (365, 80.0) // 365 is the size of vector and 80.0 is the initializer value for all elements in that array
		
		Accessing vector elements:
			vector_name.at(element_index)
			eg. test_scores.at(1)
			[imp]the keyword "at" help to access. 
			also with the "at" access, it will provide bounds check
			With only [] access, there will not be any bounds check.
			
		Adding an element to vector:
			vector_name.push_back(element);
			test_scores.push_back(80);
			
		2D vector:
			vector <vecctor<int>> movie_ratings
			{
				// 2D initialization list
			}
			
		Vector access specifiers:
			.at(<index>); //gives the vector element at the given index 
			.push_back(<element>);  //appends the element to the end the given vector.
			.size(); //gives the size of the vector
	
	Explicit typecasting
		average = static_cast<double>(total_amount)/(total_number)
			//Here the variable average is of type double, so we are explicitly coercing the operation on two integers into the type double
	
			
Eth_dev:
	0X100: Basic Introduction
	0X200: Programming
	0x240:
	
Linux: [book] 20/02/25 1900
	2.8 Memory Mappings:
		Mappings fall into two categories:
			File mapping
			Anonymous mapping
			
	2.9 Static and shared libraries:
		Static libraries work through programs that are statically linked
		shared libraries work through programs that are dynamically linked
		
	2.10 Interprocess Communication and Synchronization
		Linux uses various IPC mechanisms given below:
			Signals: used to inidicate that an event has occured
			pipes: corresponding to FIFOs, which are used to transfer data between processes
			sockets: is used to transfer data from one process to another, either on the same host computer or different hosts connected by a network.
			file locking: allows to lock regions of a file in order to prevent other processes from reading or updating the files.
			message queues: used for exchanging messages between processes 
			semaphores: is used to synchronize actions of processes
			shared memory: allows two or more processes to share a piece of memory. when one process changes the contents of the shared memory, all other connected processes can immediately see the changes
	
	2.11 Signals
		Also described as software interrupts.
		When any process receives a signal it either ignores, is killed, or suspended until later being resumed by receipt of a special-purpose signal.
		
	2.12 Threads:
	2.13 Process groups and shell job control:
	2.14 Sessions, Controlling Terminals, and controlling processes
	2.15 Pseudoterminals:	
	2.16 Date and Time:
	2.17 Client-Server Architecture:
	2.18 Realtime:
	2.19 The /proc file system:
	
	3 System Programming concepts:
	
	3.1 System calls:
		A system call changes the processor state from user mode to kernel mode, so the CPU can access the protected kernel memory
		The set of systems calls is fixed. Each system call can be identified by a unique number.
		Each system call may have a set of arguments that specify information to be transferred from user space to kernel space and vice-versa.
		
		How does a system call works:
			1. The application program makes a system call by invoking a wrapper function in the C library
			2. The wrapper function must make all of the system call arguments available to the wrapper via the stack, but the kernel expects them in specific registers. The wrapper function copies the arguments to these registers.
			3. Since all the system calls enter the kernel in the same way, the kernel needs some method of identifying the system call. To permit this, the wrapper function copies the system call number into a specific CPU register(%eax).
			4. the wrapper function executes a trap machine instruction (int 0x80), which causes the processor to switch from user mode to kernel mode and execute code pointed to by location 0x80(128 decimal) of the system's trap vector.
			5. In response to the trap to location 0x80, the kernel invokes the system_call() routine to handle the trap. This handler:
				i). Saves register values onto the kernal stack
				ii). Checks the validity of the system call number
				iii). Invokes the appropriate system call service routine, which is found by using the system call number to index a table of all system call service routines.
				iv). Restores register values from the kernel stack and places the system call return value on the stack.
				v). Returns to the wrapper function, simultaemously returning the processor to user mode.
			6. If the return value of the system call service routine indicated an error, the wrapper function sets the global variable errno using the value.The wrapper function then returns to the caller, providing an integer return value indicating the success or failure of the system call.
	
	
		

	
=====================================================
C++ queries:
	Git commands revision and notes
	Difference between structure union and class
	Virtual functions 
	Difference between data encapsulation and abstraction
	Difference between struct, class, unions, etc.
	What is member initializer lists?
	creating a vector of vectors for variable sized array
	vector functions [eg. resize]
	overloading a function in classes
	virtual functions and override keyword
	Need to understand auto keyword
	difference betwwen call by address and call by reference
	Why the below code will give compiler error?
		For the below code, what is the value of "my_array[2][1]"?
		double my_array[][] =
		 {1.2, 9.0, 3.2},
			{9.2, 0.5, 1.5, -1.2},
			{7.3, 7.9, 4.8} } ;
	Scope resolution, what is and how it works?


=================================================================================

======================================================================================================================================================================
======================================================================================================================================================================
======================================================================================================================================================================

5/2/25 Wednesday

C programming

Bitwise operators

	- bitwise operator and shift operators
	- operates in bits in integer values

Cast operator: the type cast operator has a higher precedence than all the arithmetic operators except the unary minus and unary plus operator
	(int)21.51+(int)26.99 -> 21+26

sizeof operator:
	evaluated at compile time and not runtime, unless a variable-length array is used in its argument

Operator precedence:

highest to lowest as we go to the bottom-

Postfix: () [] -> . ++ -- : Left to right
Unary: + - ! ~ ++ -- (type) * & sizeof : Right to Left 
Multiplicative: * / % : Left to right
Additive: + - : Left to right
Shift: << >> : Left to right
Relational: < <= > >= :Left to right
Equality: == != : Left to right
Bitwise AND: & : Left to right
Bitwise XOR: ^ : Left to right
Bitwise OR: | : Left to right
Logical AND: && : Left to right
Logical OR: || : Left to right
Conditional: ?: : Right to Left
Assignment: = += -= *= /= %= >>= <<= &= ^= |= : Right to Left
Comma: , : Left to right

======================================================================================================================================================================
======================================================================================================================================================================
======================================================================================================================================================================

Cpp in blockchain development

Next action:

- decide a small and simple project for initializing of blockchain development
  project 1: Basic Blockchain implementation
  Create a simple blockchain that stores data in blocks. Each block should contain: index,timestamps,data,previous block's hash,current block's hash
  outcome: understand how blocks are linked together using hashes
  tools: use c++ libraries like <openssl/sha.h> for hashing    
  
======================================================================================================================================================================
======================================================================================================================================================================
======================================================================================================================================================================

C++ in game development 

Books: SFML Game development by example,
Beginning C++ through game programming


SFML - simple and fast multimedia language

game designing is different from game programming - search on that


Game engines - learning to make one from scratch in this course
	
ECS Game programming - Entities, Components, Systems.
	Is a software design paradigm, and can be used for all software, not limited to gaming only

Object Oriented Programming - 
	Object Oriented programming used the concept of Objects and Inheritance to implement specific functionality
	Object Oriented design (in games) is the process of strictly using OOP in order to design the architecture of the game engine and the entities within it.
	
======================================================================================================================================================================
======================================================================================================================================================================
======================================================================================================================================================================

Algorithms

Divided in two sections:

Algorithms:
1. Design - writing code/design in statements and notes format. 
Those who have domain knowledge they wil make the design.
Any language can be used until its understanble by the team
It us H/W-S/W independent.
Analyze the algorithm to check is it is efficient and achieving the target purpose.

Program:
1. Implementation - converting the design to code
Programmer will write the code
This is written using only programming language
It is dependent on the H/W & OS used
Testing is required to confirm the proper working.

---------------------------------------------------------

Priori Analysis: We can equate to algorithm section as stated above. Added feature in this is the inclusion of time-space function.

Posterion Testing: We can equate it to program as stated above. Added feature in this is the inclusion of watch-time and bytes.

Info:
time-space function- 
watch-time & bytes- 

---------------------------------------------------------

Charachteristic of algorithm:

1. Input - can/should take 0 or more input
2. Output - atleast 1 output
3. Definiteness - should be non-ambiguous
4. Finiteness - duration of program should be finite
5. Effectiveness - avoid unnecessary statements and procedures

---------------------------------------------------------

How to write an Algorithm:

Any syntax can be used, but it should be understanble by all team.

criterion to consider while writing an Algorithm:
1. Time 
2. Space
3. N/W consumption and channel method
4. Power consumption
5. Number of CPU registers being consumed

==================================================================================================================================================================
==================================================================================================================================================================
==================================================================================================================================================================
